#!groovy

@Library("Infrastructure")

def type = "java"
def product = "probate"
def component = "corn-ccd-data-migration-tool"

withPipeline(type, product, component) {
  enableAksStagingDeployment()
  disableLegacyDeployment()
  nonServiceApp()
}
